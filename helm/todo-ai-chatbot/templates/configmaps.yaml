apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-ai-chatbot.backend.configmap.name" . }}
data:
  ALLOWED_ORIGINS: {{ .Values.backend.config.allowedOrigins | quote }}
  ENVIRONMENT: {{ .Values.backend.config.environment | quote }}